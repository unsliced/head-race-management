using System;
using System.Collections.Generic;

namespace Logic.Domain
{
	/*
    public class TimingPoint : ITimingPoint 
    {
        readonly string _name;
        readonly bool _isStart;
        readonly bool _isEnd;

        public TimingPoint(string name, bool isStart, bool isEnd)
        {
            _name = name;
            _isStart = isStart;
            _isEnd = isEnd;
        }

        #region ITimingPoint implementation

        public string Name
        {
            get { return _name; }
        }

        public bool IsStart
        {
            get { return _isStart; }
        }

        public bool IsEnd
        {
            get { return _isEnd; }
        }

        #endregion
    }
    */
}

